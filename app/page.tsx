"use client";
import { useCallback,useEffect,useRef,useState } from "react";
import { CalendarDays,LayoutDashboard,ListChecks,FileText,AudioLines,Plus,ArrowUpRight,Bell,ChevronRight,ArrowRight,Search,RotateCcw,ShieldCheck,CheckCircle2,Clock3,AlertCircle,type LucideIcon } from "lucide-react";
import { Sidebar,SidebarProvider,SidebarContent,SidebarHeader,SidebarFooter,SidebarTrigger,useSidebar } from "@/components/ui/sidebar";
import { AlertDialog,AlertDialogTrigger,AlertDialogContent,AlertDialogHeader,AlertDialogTitle,AlertDialogDescription,AlertDialogFooter,AlertDialogCancel,AlertDialogAction } from "@/components/ui/alert-dialog";
import { Toaster } from "@/components/ui/sonner";
import { Progress } from "@/components/ui/progress";
import { toast } from "sonner";
import { AssignmentTable,Badge,Choice,Empty,dateText,countLabel } from "./widgets";
import { CreateMeeting } from "./create-meeting";
import { MeetingDetail } from "./meeting-detail";
import { seedState,resetDemo } from "@/lib/seed";
import { loadState,saveState } from "@/lib/storage";
import { approveMeeting,dateOffset,dueSoon,localDay,statusOf,statusLabels } from "@/lib/domain";
import type { AppState,AssignmentStatus,Meeting } from "@/lib/types";
type View="overview"|"meetings"|"protocols"|"assignments"|"notifications";
const navigation:[View,LucideIcon,string][]=[["overview",LayoutDashboard,"Обзор"],["meetings",CalendarDays,"Совещания"],["protocols",FileText,"Протоколы"],["assignments",ListChecks,"Поручения"],["notifications",Bell,"Уведомления"]];
function NavButton({Icon,label,active,count,onClick}:{Icon:LucideIcon;label:string;active:boolean;count?:number;onClick:()=>void}){const {setOpenMobile}=useSidebar();return <button className={"nav-item "+(active?"active":"")} aria-current={active?"page":undefined} onClick={()=>{onClick();setOpenMobile(false)}}><Icon size={19}/>{label}{!!count&&<span className="count">{count}</span>}</button>}
export default function Home(){
 const [data,setData]=useState<AppState|null>(null),[view,setView]=useState<View>("overview"),[selected,setSelected]=useState<string|null>(null),[create,setCreate]=useState(false),[busy,setBusy]=useState(false),[saving,setSaving]=useState(false),[storageError,setStorageError]=useState(""),[loadError,setLoadError]=useState(""),[query,setQuery]=useState(""),[status,setStatus]=useState("all"),[owner,setOwner]=useState("all"),[meetingStatus,setMeetingStatus]=useState("all"),[,tick]=useState(0);
 const current=useRef<AppState|null>(null),queue=useRef(Promise.resolve()),revision=useRef(0),initializing=useRef(false);
 const init=useCallback(async()=>{if(initializing.current)return;initializing.current=true;setLoadError("");try{const stored=await loadState();if(stored&&stored.version!==1)throw new Error("Версия локальных данных не поддерживается.");const state=stored||seedState();if(!stored)await saveState(state);current.current=state;setData(state)}catch{setLoadError("Не удалось открыть локальную базу. Разрешите хранение данных для сайта и повторите.")}finally{initializing.current=false}},[]);
 useEffect(()=>{void init();const timer=setInterval(()=>tick(x=>x+1),60000);return()=>clearInterval(timer)},[init]);
 const persist=useCallback((next:AppState)=>{const r=++revision.current;setSaving(true);const task=queue.current.catch(()=>{}).then(()=>saveState(next));queue.current=task;task.then(()=>{if(r===revision.current){setSaving(false);setStorageError("")}}).catch(()=>{if(r===revision.current){setSaving(false);setStorageError("Изменения не сохранены. Проверьте свободное место или настройки браузера.")}});return task},[]);
 const commit=useCallback((update:(s:AppState)=>AppState)=>{if(!current.current)return Promise.reject(new Error("Данные ещё загружаются"));const next=update(current.current);current.current=next;setData(next);return persist(next)},[persist]);
 function changeMeeting(m:Meeting){void commit(s=>({...s,meetings:s.meetings.map(x=>x.id===m.id?m:x)})).catch(()=>{})}
 function navigate(next:View,id:string|null=null){if(busy){toast.info("Сначала остановите запись или дождитесь обработки.");return}setView(next);setSelected(id);setQuery("");setStatus("all");setOwner("all");setMeetingStatus("all")}
 function openMeeting(id:string){navigate("meetings",id)}
 function changeStatus(id:string,status:AssignmentStatus){void commit(s=>({...s,assignments:s.assignments.map(a=>a.id===id?{...a,status}:a)})).then(()=>toast.success("Статус обновлён")).catch(()=>{})}
 useEffect(()=>{const context=(document as Document&{modelContext?:{registerTool:(tool:unknown,options:unknown)=>Promise<void>|void}}).modelContext;if(!context?.registerTool)return;const lifecycle=new AbortController();try{void Promise.resolve(context.registerTool({name:"list_meeting_assignments",description:"Read the current assignments and their computed execution statuses. Does not modify data.",inputSchema:{type:"object",properties:{},additionalProperties:false},annotations:{readOnlyHint:true,untrustedContentHint:true},execute:(input:unknown)=>{if(!input||typeof input!=="object"||Array.isArray(input)||Object.keys(input).length)throw new Error("Expected an empty object");if(!current.current)throw new Error("Workspace is loading");return {assignments:current.current.assignments.map(a=>({...a,status:statusOf(a)}))}}},{signal:lifecycle.signal})).catch(()=>{})}catch{}return()=>lifecycle.abort()},[]);
 if(!data)return <main className="loading-page"><span className="brand-icon"><AudioLines/></span><h1>Протокол</h1><p>{loadError||"Открываем рабочее пространство…"}</p>{loadError&&<button className="primary" onClick={()=>void init()}>Повторить</button>}</main>;
 const notices=data.assignments.filter(a=>dueSoon(a)).sort((a,b)=>a.deadline.localeCompare(b.deadline));
 const overdue=data.assignments.filter(a=>statusOf(a)==="overdue").length;
 const review=data.meetings.filter(m=>m.protocol&&!m.protocol.approvedAt).length;
 const progress=data.assignments.filter(a=>statusOf(a)==="progress").length;
 const done=data.assignments.filter(a=>a.status==="done").length;
 const selectedMeeting=data.meetings.find(m=>m.id===selected);
 const title=navigation.find(n=>n[0]===view)![2];
 const meetingList=data.meetings.filter(m=>(view!=="protocols"||m.protocol)&&m.title.toLowerCase().includes(query.toLowerCase())&&(meetingStatus==="all"||(meetingStatus==="review"&&m.protocol&&!m.protocol.approvedAt)||(meetingStatus==="approved"&&m.protocol?.approvedAt)||(meetingStatus==="planned"&&!m.protocol))).sort((a,b)=>b.date.localeCompare(a.date));
 const assignments=data.assignments.filter(a=>(status==="all"||statusOf(a)===status)&&(owner==="all"||a.assignee===owner)&&(a.text+" "+a.department).toLowerCase().includes(query.toLowerCase()));
 function meetingRow(m:Meeting){return <button key={m.id} className="meeting-row" onClick={()=>openMeeting(m.id)}><span className="date-tile">{new Date(m.date).getDate()}<small>{new Date(m.date).toLocaleDateString("ru-RU",{month:"short"}).replace(".","").toUpperCase()}</small></span><div><h3>{m.title}</h3><p>{new Date(m.date).toLocaleTimeString("ru-RU",{hour:"2-digit",minute:"2-digit"})}<span className="meta-dot">·</span>{countLabel(m.participants.length,["участник","участника","участников"])}<span className="meta-dot">·</span>{countLabel(m.agenda.length,["вопрос","вопроса","вопросов"])}</p></div><Badge status={m.protocol?.approvedAt?"approved":m.protocol?"review":"planned"}/><ChevronRight size={17}/></button>}
 return <SidebarProvider><Sidebar className="no-print"><SidebarHeader><div className="brand"><span className="brand-icon"><AudioLines size={23}/></span>протокол<span className="brand-dot">.</span></div><div className="workspace-label">СИСТЕМА ЭЛЕКТРОННОГО ДОКУМЕНТООБОРОТА</div></SidebarHeader><SidebarContent><div className="nav-caption">РАБОЧЕЕ ПРОСТРАНСТВО</div>{navigation.map(([id,Icon,label])=><NavButton key={id} Icon={Icon} label={label} active={view===id} count={id==="notifications"?notices.length:id==="assignments"?data.assignments.length:undefined} onClick={()=>navigate(id)}/>)}</SidebarContent><SidebarFooter><div className="demo-note"><span className="green-dot"/>Демонстрационный режим<small>Данные хранятся в вашем браузере</small></div><AlertDialog><AlertDialogTrigger asChild><button className="reset-button" disabled={busy}><RotateCcw size={14}/>Восстановить демоданные</button></AlertDialogTrigger><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Восстановить демоданные?</AlertDialogTitle><AlertDialogDescription>Изменения и аудиозаписи в демонстрационных совещаниях будут удалены. Созданные вами совещания и их поручения сохранятся.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Отмена</AlertDialogCancel><AlertDialogAction onClick={()=>{void commit(resetDemo).then(()=>toast.success("Демоданные восстановлены")).catch(()=>{});setSelected(null)}}>Восстановить</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog><div className="profile"><span className="avatar">АС</span><div>Анна Смирнова<small>Секретарь · демопрофиль</small></div></div></SidebarFooter></Sidebar><div className="app-body"><header className="topbar no-print"><div><SidebarTrigger aria-label="Меню навигации"/><span className="breadcrumb-root">Рабочее пространство</span><ChevronRight size={13}/><strong>{title}</strong></div><div><span className="header-date">{new Date().toLocaleDateString("ru-RU",{day:"numeric",month:"long",year:"numeric"})}</span><button className="notification-button" aria-label={`Уведомления: ${notices.length}`} onClick={()=>navigate("notifications")}><Bell size={19}/>{notices.length>0&&<i/>}</button><span className="avatar small">АС</span></div></header><main className="content">{storageError&&<div className="error-note no-print" role="alert">{storageError} <button className="text-link" onClick={()=>current.current&&void persist(current.current).catch(()=>{})}>Повторить сохранение</button></div>}{selectedMeeting?<MeetingDetail key={selectedMeeting.id} meeting={selectedMeeting} onChange={changeMeeting} onBack={()=>navigate("meetings")} busy={busy} onBusy={setBusy} onApprove={()=>{void commit(s=>approveMeeting(s,selectedMeeting.id)).then(()=>toast.success("Протокол утверждён. Поручения созданы.")).catch(()=>toast.error("Не удалось сохранить утверждение. Повторите сохранение."))}}/>:<><div className="page-heading"><div><div className="eyebrow">{view==="overview"?"ВСЁ ПОД КОНТРОЛЕМ":"РАБОЧЕЕ ПРОСТРАНСТВО"}</div><h1>{title}</h1><p>{view==="overview"?"От обсуждения — к решениям и выполненным поручениям.":view==="meetings"?"Встречи, повестки и решения вашей команды.":view==="protocols"?"Проверьте проекты и утвердите решения совещаний.":view==="assignments"?"Ответственные, сроки и ход исполнения решений.":"Ближайшие сроки и поручения, требующие внимания."}</p></div>{(view==="overview"||view==="meetings")&&<button className="primary" onClick={()=>setCreate(true)}><Plus size={17}/>Новое совещание</button>}</div>{view==="overview"?<><section className="stats">{([["Совещания",data.meetings.length,CalendarDays,"В рабочем пространстве","meetings"],["На проверке",review,FileText,"Проекты протоколов","protocols"],["В работе",progress,ListChecks,"Активные поручения","assignments"],["Просрочено",overdue,Clock3,"Требуют внимания","assignments"]] as [string,number,LucideIcon,string,View][]).map(([label,n,Icon,note,v],i)=><button className={`stat stat-${i}`} key={label} onClick={()=>{navigate(v);if(i===1)setMeetingStatus("review");if(i===2)setStatus("progress");if(i===3)setStatus("overdue")}}><span>{label}<span className="stat-icon"><Icon size={18}/></span></span><strong>{String(n).padStart(2,"0")}</strong><small>{i===3&&n>0?<span className="red-text">Проверьте сроки исполнения</span>:note}</small></button>)}</section><div className="dashboard-grid"><div><section className="meeting-banner"><div><span className="eyebrow">ИНТЕЛЛЕКТУАЛЬНЫЙ ПРОТОКОЛ</span><h2>Встречи заканчиваются.<br/>Решения продолжают работать.</h2><p>Запись, протокол и поручения — в одном месте.</p><button className="banner-button" onClick={()=>{const m=data.meetings.find(m=>m.protocol&&!m.protocol.approvedAt);if(m)openMeeting(m.id);else setCreate(true)}}>Перейти к протоколу <ArrowUpRight size={15}/></button></div><div className="wave-art" aria-hidden="true">{Array.from({length:29},(_,i)=><i key={i} style={{height:12+Math.abs(Math.sin(i*1.4)*Math.cos(i*.24))*90}}/>)}</div></section><section className="panel"><div className="section-heading"><h2>Последние совещания</h2><button className="text-link" onClick={()=>navigate("meetings")}>Все совещания<ArrowRight size={14}/></button></div>{[...data.meetings].sort((a,b)=>b.date.localeCompare(a.date)).slice(0,4).map(meetingRow)}</section></div><aside className="dashboard-aside"><section className="panel upcoming"><div className="section-heading"><h2>Ближайшие сроки</h2><span className="number-count">{notices.length}</span></div><div className="aside-subtitle">Сегодня и в ближайшие 3 дня</div>{notices.length?notices.slice(0,4).map(a=><button className="deadline-item" key={a.id} onClick={()=>{navigate("assignments");setQuery(a.text)}}><span className={`deadline-dot ${statusOf(a)==="overdue"?"red":""}`}/><div><h3>{a.text}</h3><p>{a.assignee}</p><span className={statusOf(a)==="overdue"?"overdue":"deadline-date"}><Clock3 size={12}/>{dateText(a.deadline)}{statusOf(a)==="overdue"?" · Просрочено":""}</span></div></button>):<Empty title="Всё спокойно" description="Срочных поручений нет."/>}<button className="aside-link" onClick={()=>navigate("notifications")}>Все уведомления<ArrowRight size={14}/></button></section><section className="completion-card"><div><ShieldCheck size={20}/><h3>Исполнение решений</h3></div><div className="completion-value">{data.assignments.length?Math.round(done/data.assignments.length*100):0}<span>%</span></div><Progress className="completion-track" aria-label="Доля исполненных поручений" value={data.assignments.length?done/data.assignments.length*100:0}/><p>{done} из {data.assignments.length} поручений исполнено</p></section></aside></div><section className="panel tasks-panel"><div className="section-heading"><h2>Поручения на контроле</h2><button className="text-link" onClick={()=>navigate("assignments")}>Все поручения<ArrowRight size={14}/></button></div><AssignmentTable items={data.assignments.filter(a=>a.status!=="done").sort((a,b)=>a.deadline.localeCompare(b.deadline)).slice(0,4)} onStatus={changeStatus} onMeeting={openMeeting} compact/></section></>:view==="meetings"||view==="protocols"?<section className="panel"><div className="list-toolbar"><label className="search-box"><Search size={17}/><input aria-label="Поиск совещаний" placeholder="Найти совещание…" value={query} onChange={e=>setQuery(e.target.value)}/></label><Choice value={meetingStatus} onChange={setMeetingStatus} label="Статус совещания" items={[["all","Все статусы"],["review","На проверке"],["approved","Утверждён"],...(view==="meetings"?[["planned","Запланировано"] as [string,string]]:[])]}/><span className="list-count">Найдено: {meetingList.length}</span></div>{meetingList.length?meetingList.map(meetingRow):<Empty title="Совещания не найдены" description="Измените фильтры или создайте новое совещание."/>}</section>:view==="assignments"?<section className="panel"><div className="list-toolbar"><label className="search-box"><Search size={17}/><input aria-label="Поиск поручений" placeholder="Найти поручение…" value={query} onChange={e=>setQuery(e.target.value)}/></label><Choice value={status} onChange={setStatus} label="Фильтр статуса" items={[["all","Все статусы"],...Object.entries(statusLabels)]}/><Choice value={owner} onChange={setOwner} label="Фильтр ответственного" items={[["all","Все ответственные"],...Array.from(new Set(data.assignments.map(a=>a.assignee))).map(n=>[n,n] as [string,string])]}/></div><AssignmentTable items={assignments} onStatus={changeStatus} onMeeting={openMeeting}/><div className="table-footer">Поручений: {assignments.length}<span>Просрочка рассчитывается автоматически</span></div></section>:<section className="panel"><div className="section-heading"><h2>Требуют внимания</h2><span className="badge">{notices.length}</span></div>{notices.length?notices.map(a=><button className="notification-row" key={a.id} onClick={()=>{navigate("assignments");setQuery(a.text)}}><span className={`notice-icon ${statusOf(a)==="overdue"?"red":""}`}><AlertCircle size={21}/></span><div><h3>{statusOf(a)==="overdue"?"Срок исполнения истёк":"Приближается срок исполнения"}</h3><p>{a.text}</p><small>{a.assignee} · {dateText(a.deadline)}</small></div><ChevronRight size={17}/></button>):<Empty title="Нет срочных уведомлений" description="Здесь появятся поручения со сроком в ближайшие три дня и просроченные задачи."/>}</section>}</>}<footer className="workspace-footer no-print"><span><ShieldCheck size={13}/>Данные остаются в вашем браузере</span><span aria-live="polite">{storageError?"Ошибка сохранения":saving?"Сохраняем…":"Сохранено локально"}</span></footer></main></div><CreateMeeting key={String(create)} open={create} onOpenChange={setCreate} onCreate={m=>{void commit(s=>({...s,meetings:[m,...s.meetings]})).then(()=>toast.success("Совещание создано")).catch(()=>{});setCreate(false);openMeeting(m.id)}}/><Toaster position="bottom-right" richColors/></SidebarProvider>
}


